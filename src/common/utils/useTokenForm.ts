/*
import { useLocation } from 'react-router-dom';

const SET_NEW_PASSWORD= "/set-new-password/"

export const useTokenFromUrl=() => {
    const { pathname } = useLocation();
    const tokenIndex = pathname.indexOf(SET_NEW_PASSWORD) + SET_NEW_PASSWORD.length;
    return pathname.substring(tokenIndex);
}*/

export {}